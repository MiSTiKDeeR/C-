Виконати задачі з розділу 1 (задача 6, 7, 8, 9):
https://drive.google.com/drive/u/1/folders/1CxCb9PI_TSdCYTCZj-chVZkL7qYiC_dN

Номер 6
#include <iostream>
using namespace std;
int main(void) {
    bool answer;
    int value;
    cout << "Enter a value: ";
    cin >> value;
    answer = (value>=0 && value<10)(value*2<20 && value-2>-2)(value-1>1 && value/2<10)||(value==111); // insert your expression here
    cout << (answer ? "THAT'S TRUE :)" : "THAT'S NOT TRUE :(") << endl;
    return 0;
}

Номер 7
#include <iostream>
using namespace std;
int main(void){
    float a1= 2.3;
    float a2= 2.3;
    float a3= 2.123456;
    float a4= 2.123456;
    float a5= 2.123456;
    cout << a1 <<'\n';
    cout << a2 <<"0"<<'\n';
    cout << a3 <<'\n';
    cout << round (a4 * 100)/100 <<'\n';
    cout << int (a5) <<'\n';
}

Номер 8 
#include <iostream>
#include<math.h>

using namespace std;

int main()
{
    int a,b,c,d;
    cin>>a;
    cin>>b;
    
    c=a/a;
    d=a/b;
    if(c==d){
     cout<<"Results are equal (by 0.000001 epsilon)";
    }
    else{
     cout<<"Results are not equal (by 0.000001 epsilon)";
    }
}

Номер 9

#include <algorithm>
#include <iostream>
#include <cmath>

using namespace std;

int main(){
      int a,b,c,d;
      cin>>a;
      cin>>b;
      cin>>c;
      cin>>d;
      if(a<1){
        cout<<"error";
        return 0;
      } else if(a>255){
        cout<<"error";  
        return 0;
      }
      if(b<1){
        cout<<"error";  
        return 0;
      } else if(b>255){
        cout<<"error"; 
        return 0;
      }
      if(c<1){
        cout<<"error"; 
        return 0;
      } else if(c>255){
        cout<<"error";  
        return 0;
      }
      if(d<1){
        cout<<"error";  
        return 0;
      } else if(d>255){
        cout<<"error";  
        return 0;
      }
      cout<<a<<"."<<b<<"."<<c<<"."<<d;
      return 0;
}
